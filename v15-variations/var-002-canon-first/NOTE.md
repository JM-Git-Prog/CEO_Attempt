# var-002 — canon-first + staged LINE (pre-reconcile)

Snapshotted 2026-07-30 before adding: SAM3 blueprint reconciliation with
pre-verdict (GREEN/AMBER/RED), the ⑂ forward/branch button, and the sessions
panel.

Flow in this variation: prompt → canon FIRST (approve / re-dream / reword,
← back button) → world + LINE dock (cut → complete → photo verdict → object
canon → mesh verdict → paint verdict → seat) — but the blueprint is still the
LLM's unmeasured guess in this variation.

To rewind here: double-click `RESTORE-THIS-VARIATION.bat`, then restart via
START-LIVING-ROOM-8000.bat and refresh the v15_Fable tab.

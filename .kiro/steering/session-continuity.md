# Session Continuity — Unified World Pipeline

For the active `.kiro/specs/unified-world-pipeline/` specification:

## Execution truth and evidence precedence

1. Treat the active `requirements.md`, `design.md`, and especially `tasks.md` as execution truth and dependency order.
2. Treat validation evidence as authoritative only for the exact revision and candidate-tree fingerprint it names. A newer relevant change makes the candidate unvalidated without erasing the last validated baseline.
3. Treat session evidence as authoritative only when the active specification marks that session eligible for the claimed profile and milestone.
4. Treat KiroGraph memory, continuation notes, wake logs, and prior narratives as historical context. They can explain decisions and locate evidence, but they cannot override active tasks, exact-fingerprint validation, or session eligibility.
5. Preserve the former `llm-driven-upbge-runtime` Task 10 continuation only as superseded history. It is not current execution truth and must never reactivate old Task 10 or downstream Tasks 13–14.

## Visual-first critical path

Execute this order exactly; do not jump ahead:

1. Complete Task 11.8 counter/cabinet repair and bind revalidation to the repaired candidate fingerprint.
2. Freeze exploratory geometry work.
3. Run one fixed-chair bake-off under a fixed 60–90 minute active budget: raw crop, the existing Qwen amodal completion, and video-depth only if that evidence is already available. Do not download or integrate MiniMax, DA3, MoGe, Anima, or another model for this milestone.
4. Evaluate every chair lane with one common StandaloneAssetGate and choose the visually best passing lane.
5. Produce and human-approve five independently loadable standalone assets: round table, chair-1, chair-2, built-in counter/cabinet, and coffee maker.
6. Assemble one golden Browser room from the approved assets and the approved MetricPlan/CameraContract.
7. Require a GREEN SceneVisualGate, basic playability, one functional UUID-bound GAME interaction, and at least one working REAL read-only binding.
8. Record the exact-fingerprint `Demo Ready` milestone.
9. Only after Demo Ready, run a replacement brand-new empty Task 11.7 V16 zero-state session with the exact canonical prompt; only after that clean pass may Tasks 11.9–11.11 perform repeated release qualification.

The product stage order is: Prompt → Dream Preview → MetricPlan/Blockout approval → geometry-conditioned final Scene Canon → object production. Canon, depth, video, masks, and generated meshes remain evidence or appearance candidates and never replace MetricPlan spatial authority.

## Readiness and profile boundaries

- **Demo Ready** means the golden Browser room passes StandaloneAssetGate and SceneVisualGate, is playable, demonstrates one functional GAME interaction and one REAL read-only binding, and has exact-fingerprint evidence. It is not release evidence.
- **Release Ready** additionally requires the replacement clean Task 11.7 run and all fresh repeated qualification required by Tasks 11.9–11.11.
- **Platform Complete** is broader post-MVP completion and is not implied by Demo Ready or Release Ready.
- Diagnostic and demo profiles may reuse approved warehouse assets only when their recorded profile permits it. Fresh-benchmark and release profiles remain always-fresh and may not use prior-session assets as qualifying evidence.

## Preservation and ownership

- Preserve V3–V16 behavior, selectors/routes, version-switch links, and unrelated working-tree changes.
- Preserve all failed or restored sessions append-only as diagnostic evidence; they are permanently ineligible for Demo Ready or release qualification and must never be resumed as proof.
- Task 11.8 repairs and revalidates the demonstrated defect but does not automatically start a replacement session before Demo Ready.
- Windows owns the Ratchet watch process through its Scheduled Task and keepalive scripts. Do not hold it in an agent-managed terminal. Comfy Desktop remains the port-8188 owner; do not launch or retain ComfyUI in an agent-managed terminal.
- Use local Ollama only for bounded drafting or screening; never use cloud offload without explicit permission. Model output is evidence, not release authority.
- At each progress checkpoint, confirm the current work still advances the 6–8 active-coding-hour MVP target. Defer non-blocking polish, tooling, and model exploration when it does not.

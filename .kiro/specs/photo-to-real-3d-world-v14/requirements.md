# Requirements Document

## Introduction

This specification defines the "Photo-to-Real-3D-World V14" pipeline — a major upgrade to the existing photo-to-playable-world system that replaces primitive placeholder geometry with real textured 3D meshes generated by Hunyuan3D 2.1 (with Trellis2 fallback), introduces a persistent Asset Warehouse for cataloging generated assets, delivers a new V14 web interface using Three.js GLTFLoader for real mesh rendering, implements two-pass PBR material quality, adds dynamic physics based on estimated weight, and enforces strict VRAM management for the RTX 4090 24GB constraint.

The system always generates fresh meshes per photo (no reuse lookup). The existing photo pipeline infrastructure (`src/photo_pipeline/`) provides segmentation, depth estimation, layout, physics settle, and WorldContract assembly — this spec extends it with real mesh generation, asset cataloging, enhanced materials, and a browser-based 3D viewer.

### Environment Assumptions

- **NVIDIA RTX 4090 (24GB VRAM), 96GB system RAM, Windows 11.** Local GPU inference with flash attention enabled.
- **ComfyUI on localhost:8188** with Hunyuan3D 2.1 (ImageOnlyCheckpointLoader → ModelSamplingAuraFlow → CLIPVisionEncode → Hunyuan3Dv2Conditioning → KSampler → VAEDecodeHunyuan3D → VoxelToMesh → SaveGLB), Trellis2, SAM ViT-H, FLUX.2 Klein, and Depth Anything 3 nodes installed.
- **Ollama with flash attention enabled** for semantic labeling and metadata extraction.
- **UPBGE 0.50 installed** for optional high-quality compilation.
- **Existing photo pipeline** (`src/photo_pipeline/`) handles segmentation, depth, layout, physics settle, and WorldContract assembly.
- **Single-user, single-session-at-a-time** pipeline execution.
- **No cloud API calls** — all inference runs locally.
- **Always fresh generation** — no asset reuse lookup between sessions.

## Glossary

- **Photo_Pipeline_V14**: The upgraded end-to-end pipeline that produces real textured 3D meshes from a single photograph and renders them in a browser-based Three.js viewer
- **Source_Image**: The single RGB photograph of an indoor room provided as input
- **Hunyuan3D_Generator**: The primary 3D mesh generation stage using the Hunyuan3D 2.1 model via ComfyUI (ImageOnlyCheckpointLoader → ModelSamplingAuraFlow → CLIPVisionEncode → Hunyuan3Dv2Conditioning → KSampler → VAEDecodeHunyuan3D → VoxelToMesh → SaveGLB workflow)
- **Trellis2_Generator**: The fallback 3D mesh generation stage using Microsoft Trellis2 4B model via ComfyUI when Hunyuan3D fails
- **Object_PNG**: An isolated RGBA image of a single segmented object on transparent background, produced by SAM segmentation
- **Room_Shell_Mesh**: A textured GLB mesh representing the room environment (floor, walls, ceiling) reconstructed from a depth-displaced grid, textured with the inpainted Room_Plate
- **Depth_Grid_Mesh**: The room shell reconstruction method that converts a depth map into a displaced-grid mesh (NOT Poisson reconstruction)
- **Asset_Warehouse**: The persistent, append-only, session-independent library that catalogs every generated GLB asset with extensive metadata
- **Asset_Registry**: The JSON metadata file per asset stored in the Asset_Warehouse containing name, semantic label, category, era, condition, material type, dimensions, weight estimate, generation method, source photo hash, and source session
- **VRAM_Manager**: The component that enforces sequential model loading/unloading to prevent VRAM exhaustion on the RTX 4090 (24GB)
- **Two_Pass_Materials**: The material quality strategy where Pass 1 applies immediate photo-projected textures and Pass 2 estimates full PBR parameters (metallic, roughness, normal) in the background when GPU is free
- **V14_Interface**: The new web interface version using Three.js GLTFLoader for rendering real textured meshes in-browser, replacing primitive box rendering from V13
- **GLTFLoader**: The Three.js loader that imports GLB files with embedded PBR textures for real-time WebGL rendering
- **Dynamic_Physics**: The physics classification system that assigns dynamic (grabbable/pushable) or static (immovable) physics bodies based on estimated object weight
- **Flash_Attention**: The memory-efficient attention mechanism enabled in both ComfyUI and Ollama to reduce VRAM peak usage
- **PBR_Materials**: Physically-Based Rendering material properties (base color, metallic, roughness, normal map) embedded in GLB files for realistic Three.js rendering
- **Semantic_Label**: A human-readable category label assigned to each object by Ollama vision analysis (e.g., "wooden dining chair", "ceramic floor vase")
- **Game_Industry_Taxonomy**: The asset categorization system following standard game development conventions: Props/Static Meshes, Foliage/Flora, Hard Surface Assets, Set Dressing/Decor, Modular Architecture Kit Pieces

## Requirements

### Requirement 1: Real 3D Mesh Generation via Hunyuan3D 2.1

**User Story:** As a user, I want each segmented object to be converted into a real textured 3D mesh using Hunyuan3D 2.1, so that the 3D world contains detailed geometry instead of primitive boxes.

#### Acceptance Criteria

1. WHEN the Hunyuan3D_Generator receives an Object_PNG, THE Hunyuan3D_Generator SHALL submit the proven ComfyUI workflow (ImageOnlyCheckpointLoader → ModelSamplingAuraFlow → CLIPVisionEncode → Hunyuan3Dv2Conditioning → KSampler at 50 steps, cfg=7.0 → VAEDecodeHunyuan3D at octree_resolution=384 → VoxelToMesh → SaveGLB) and produce a textured GLB mesh
2. WHEN Hunyuan3D 2.1 generation succeeds, THE Hunyuan3D_Generator SHALL validate the output mesh contains at least 100 faces, at least 50 vertices, and embedded texture data before accepting the result
3. IF Hunyuan3D 2.1 fails or produces a mesh failing validation, THEN THE Photo_Pipeline_V14 SHALL attempt generation using the Trellis2_Generator as fallback — there is no per-object time limit that sacrifices quality (only a 180-second stall-detection timeout triggers fallback)
4. WHEN the Trellis2_Generator receives an Object_PNG, THE Trellis2_Generator SHALL submit the Trellis2 ComfyUI workflow (Trellis2LoadModel → Trellis2PreProcessImage → Trellis2MeshWithVoxelGenerator at 18 steps → Trellis2SimplifyMesh at 12000 triangles → Trellis2ExportMesh as GLB with embedded textures) and produce a textured GLB mesh
5. IF both Hunyuan3D 2.1 and Trellis2 fail for an object, THEN THE Photo_Pipeline_V14 SHALL produce a Placeholder_Geometry primitive (box, cylinder, or sphere by aspect ratio) textured with the average color from the Object_PNG
6. THE Hunyuan3D_Generator SHALL record generation method (hunyuan3d_v2.1, trellis2, or placeholder), generation time in seconds, face count, and vertex count for each object in the pipeline manifest
7. THE Hunyuan3D_Generator SHALL use maximum quality settings (50 steps, cfg=7.0, octree_resolution=384) to produce the best possible mesh topology — generation time of 60-90 seconds per object is acceptable

### Requirement 2: VRAM Management and Sequential Model Loading

**User Story:** As a developer, I want strict VRAM management that prevents out-of-memory crashes on the RTX 4090 24GB, so that the pipeline completes reliably without manual intervention.

#### Acceptance Criteria

1. THE VRAM_Manager SHALL enforce that FLUX and Hunyuan3D models are never loaded simultaneously in GPU VRAM — the pipeline SHALL fully unload FLUX before loading Hunyuan3D, and fully unload Hunyuan3D before loading FLUX
2. WHEN transitioning between GPU models, THE VRAM_Manager SHALL call the ComfyUI `/free` endpoint and wait until VRAM usage drops below 4GB before loading the next model
3. THE VRAM_Manager SHALL execute GPU-intensive stages in a fixed sequential order: SAM segmentation → FLUX inpainting → model unload → Depth Anything 3 → model unload → Hunyuan3D 2.1 per object (sequential) → model unload
4. WHILE a GPU model is loaded, THE VRAM_Manager SHALL enable flash attention to reduce peak VRAM usage below 22GB for any single inference call
5. IF a ComfyUI inference call returns a VRAM out-of-memory error, THEN THE VRAM_Manager SHALL call the `/free` endpoint, wait 5 seconds for memory release, and retry the inference call exactly once before falling to the next method in the fallback chain
6. THE VRAM_Manager SHALL allow CPU-bound stages (layout estimation, physics settle, metadata extraction via Ollama) to execute in parallel with GPU stages only when no GPU model transition is in progress
7. WHEN system RAM usage exceeds 80GB (of 96GB available), THE VRAM_Manager SHALL pause new stage submissions until memory drops below 72GB, using the 96GB system RAM as spillover buffer for intermediate tensors

### Requirement 3: Room Shell Reconstruction from Depth

**User Story:** As a user, I want the room environment to be a real textured mesh reconstructed from the photo's depth information, so that walls, floor, and ceiling have accurate geometry matching the photographed space.

#### Acceptance Criteria

1. WHEN the depth map is available from Depth Anything 3, THE Photo_Pipeline_V14 SHALL reconstruct the Room_Shell_Mesh using a displaced-grid method: create a regular grid mesh at the source image resolution (or downsampled to maximum 500×500 vertices), displace each vertex along the camera ray by its depth value, and export as GLB
2. THE Room_Shell_Mesh SHALL be textured with the inpainted Room_Plate image using UV coordinates that map directly from the grid vertex positions to the corresponding pixel locations in the Room_Plate
3. THE Room_Shell_Mesh SHALL NOT use Poisson surface reconstruction — the displacement grid method preserves the direct relationship between pixels and mesh vertices for accurate texture mapping
4. WHEN the Room_Shell_Mesh is generated, THE system SHALL orient the mesh in WorldContract coordinates (right-handed, Y-up, meters) with the camera viewpoint at the origin looking along negative-Z
5. IF Depth Anything 3 fails or produces a depth map where more than 50% of pixels have invalid values (zero, negative, or infinite), THEN THE system SHALL fall back to a flat box room (4m depth, width from aspect ratio, 2.7m ceiling height) textured with the Room_Plate
6. THE Room_Shell_Mesh SHALL have vertex count between 10000 and 250000 to balance reconstruction detail against WebGL rendering performance in the V14_Interface

### Requirement 4: Object Placement via Depth Back-Projection

**User Story:** As a user, I want objects placed at their original positions from the photograph, so that the 3D room layout matches the real scene.

#### Acceptance Criteria

1. WHEN object meshes and the depth map are available, THE Layout_Estimator SHALL compute each object's 3D position by back-projecting the object's 2D centroid (from segmentation mask) through the camera model using the metric depth value at that pixel coordinate
2. THE Layout_Estimator SHALL apply the back-projection formula: x = (u - cx) × d / fx, y = -(v - cy) × d / fy, z = -d, where (u,v) is the pixel centroid, d is depth, (cx,cy) is the principal point, and (fx,fy) are focal lengths
3. WHEN initial 3D positions are computed for all objects, THE Layout_Estimator SHALL run a physics settle pass (gravity simulation, maximum 500 iterations or 5 seconds wall time) to resolve floating objects and interpenetration
4. THE Layout_Estimator SHALL clamp each object's final position to within the Room_Shell_Mesh bounding volume with a 0.05m margin to prevent objects from appearing outside the room
5. IF the depth value at an object's centroid is invalid (zero or infinite), THEN THE Layout_Estimator SHALL estimate depth by averaging valid depth values within the object's mask region

### Requirement 5: Two-Pass Material Quality

**User Story:** As a user, I want objects to have realistic materials that improve over time, so that I get fast initial results with photo-projected textures followed by high-quality PBR materials when the GPU becomes free.

#### Acceptance Criteria

1. WHEN a 3D mesh is generated for an object, THE Two_Pass_Materials system SHALL immediately apply Pass 1 textures by projecting the Object_PNG onto the mesh surface using the camera model (photo-projection), producing a usable textured GLB within 2 seconds of mesh generation
2. WHEN all objects have Pass 1 textures applied and the GPU is free (no active inference tasks), THE Two_Pass_Materials system SHALL queue Pass 2 PBR estimation for each object in background priority order (largest objects first)
3. WHEN Pass 2 processes an object, THE Two_Pass_Materials system SHALL estimate metallic (0.0-1.0), roughness (0.0-1.0), and normal map parameters from the Object_PNG using a material classification heuristic and update the GLB file with embedded PBR textures
4. THE V14_Interface SHALL display Pass 1 textures immediately and hot-swap to Pass 2 PBR materials via WebSocket notification when background processing completes, without requiring page refresh
5. IF Pass 2 estimation fails for an object, THEN THE system SHALL retain the Pass 1 photo-projected texture and log a warning — the object remains visually acceptable with projected color
6. THE Two_Pass_Materials system SHALL record material pass status per object in the pipeline manifest (pass1_complete, pass2_complete, pass2_failed)

### Requirement 6: Dynamic Physics Based on Estimated Weight

**User Story:** As a user, I want light objects in the scene to be grabbable and pushable while heavy objects remain fixed, so that physics interactions feel realistic based on what objects look like.

#### Acceptance Criteria

1. WHEN assembling the WorldContract, THE Photo_Pipeline_V14 SHALL classify each object as dynamic (grabbable/pushable) or static (immovable) based on its estimated mass: objects with estimated mass at or below 25kg SHALL be dynamic, objects above 25kg SHALL be static
2. THE Photo_Pipeline_V14 SHALL estimate object mass using the formula: mass_kg = volume_m3 × material_density, where volume is computed from the Scale_Calibrator dimensions and material_density is looked up from the semantic label (wood=600, metal=7800, glass=2500, fabric=200, ceramic=2300, plastic=950 kg/m³)
3. WHEN an object is classified as dynamic, THE WorldContract physics intent SHALL include: body_mode=DYNAMIC, mass_kg set to the estimated value, friction=0.5, restitution=0.2, can_topple=True
4. WHEN an object is classified as static, THE WorldContract physics intent SHALL include: body_mode=STATIC, mass_kg=0, friction=0.6, restitution=0.1, can_topple=False
5. THE physics classification SHALL override to STATIC for any object whose semantic label indicates architectural function (walls, doors, built-in shelving, countertops, large appliances) regardless of estimated mass
6. THE pipeline manifest SHALL record estimated_mass_kg, material_density_used, volume_m3, and physics_classification (dynamic or static) for each object

### Requirement 7: Asset Warehouse — Persistent Modular Asset Library

**User Story:** As a user, I want every generated 3D asset to be cataloged into a persistent library organized by game industry categories, so that I accumulate a growing collection of assets across sessions.

#### Acceptance Criteria

1. WHEN a 3D mesh is successfully generated (via Hunyuan3D or Trellis2, not placeholder), THE Asset_Warehouse SHALL save the GLB file into the appropriate category directory: `assets/props/`, `assets/architecture/`, `assets/foliage/`, `assets/hard-surface/`, or `assets/set-dressing/`
2. WHEN cataloging an asset, THE Asset_Warehouse SHALL create a JSON metadata registry file alongside the GLB containing: name (filename-safe), semantic_label (from Ollama vision), category (game industry taxonomy), era (estimated period), condition (new/worn/broken), working_status (working/non-working/not-applicable), material_type (primary material), dimensions_m (width/height/depth), weight_estimate_kg, generation_method (hunyuan3d_v2.1 or trellis2), source_photo_hash (SHA-256 of Source_Image), source_session_id, face_count, vertex_count, has_pbr_textures (boolean), and created_at (ISO timestamp)
3. THE Asset_Warehouse SHALL assign categories following game industry taxonomy: Props (furniture, everyday objects), Architecture (structural elements, doors, windows), Foliage (plants, flowers, trees), Hard Surface (mechanical objects, electronics, appliances), Set Dressing (decorative items, art, books, small accessories)
4. THE Asset_Warehouse SHALL be append-only and session-independent — assets persist across sessions and are never overwritten or deleted by the pipeline
5. THE Asset_Warehouse SHALL use Ollama vision analysis to generate the semantic_label, era, condition, and working_status fields by sending the Object_PNG with a structured prompt requesting JSON output
6. WHEN the Asset_Warehouse directory structure does not exist, THE system SHALL create it on first asset save with the five category subdirectories
7. THE Asset_Warehouse SHALL generate unique filenames using the pattern `{semantic_label_slug}_{session_id_short}_{mask_id}.glb` to prevent collisions across sessions

### Requirement 8: V14 Web Interface with Three.js GLTFLoader

**User Story:** As a user, I want a new web interface that renders real textured 3D meshes in my browser using Three.js, so that I can see the actual generated geometry instead of primitive colored boxes.

#### Acceptance Criteria

1. THE V14_Interface SHALL load generated GLB files using Three.js GLTFLoader and render them with PBR materials (metallic-roughness workflow) in a WebGL scene with orbit camera controls
2. THE V14_Interface SHALL render the Room_Shell_Mesh as the environment and each Object_Mesh at its WorldContract position/rotation, producing a complete 3D scene matching the original photograph's layout
3. THE V14_Interface SHALL support first-person navigation controls (WASD movement, mouse look) in addition to orbit mode, allowing the user to walk through the reconstructed room
4. WHEN a new photo pipeline session completes, THE V14_Interface SHALL receive mesh URLs via Server-Sent Events and progressively load each GLB as it becomes available, displaying objects incrementally rather than waiting for all generation to finish
5. THE V14_Interface SHALL display a loading progress indicator showing: current stage name, objects completed vs total, elapsed time, and estimated time remaining
6. THE V14_Interface SHALL be accessible at the existing web server URL with query parameter `?v=14` and SHALL become the default version when no version parameter is supplied
7. WHEN Pass 2 PBR materials complete for an object, THE V14_Interface SHALL hot-swap the object's material via WebSocket notification without reloading the full scene

### Requirement 9: Pipeline Orchestration and Timing

**User Story:** As a developer, I want the V14 pipeline to run end-to-end reliably without time pressure, prioritizing quality over speed, so that every generated mesh is the best the hardware can produce.

#### Acceptance Criteria

1. THE Photo_Pipeline_V14 SHALL complete all stages (segmentation through WorldContract assembly and initial Three.js scene load) reliably for scenes containing up to 15 segmented objects on the RTX 4090 — there is no hard time cap that sacrifices quality
2. THE Photo_Pipeline_V14 SHALL execute stages in the required VRAM-safe order: SAM segmentation → FLUX inpainting → FLUX unload → Depth Anything 3 → depth unload → Hunyuan3D 2.1 per object sequentially (at maximum quality settings: 50 steps, octree_resolution 384) → Hunyuan3D unload → Pass 1 textures → layout + physics settle → WorldContract assembly
3. THE Photo_Pipeline_V14 SHALL use maximum quality settings for mesh generation: KSampler steps=50 (not 30), octree_resolution=384 (not 256), and cfg=7.0 for sharper detail — accepting longer generation time per object (~60-90s) for significantly better mesh topology
4. THE Photo_Pipeline_V14 SHALL emit SSE progress events at each stage transition and per-object completion, delivering each event within 2 seconds of the state change, with accurate elapsed time and "X of N objects complete" counters
5. THE Photo_Pipeline_V14 SHALL NOT terminate or time out based on wall-clock duration — if Hunyuan3D takes 90 seconds per object and there are 15 objects, the pipeline runs for 22+ minutes and that is acceptable
6. THE Photo_Pipeline_V14 SHALL begin Pass 2 PBR material estimation only after the V14_Interface has loaded and displayed all Pass 1 textured meshes — Pass 2 runs as a non-blocking background enhancement with no time pressure
7. IF a single object's Hunyuan3D generation exceeds 180 seconds (indicating a stalled inference), THEN the pipeline SHALL fall to Trellis2 for that object only — this is an error recovery timeout, not a quality tradeoff

### Requirement 10: Always Fresh Generation

**User Story:** As a user, I want every photo submission to produce freshly generated meshes without reusing previously generated assets, so that each reconstruction is unique to the submitted photograph.

#### Acceptance Criteria

1. WHEN a Source_Image is submitted, THE Photo_Pipeline_V14 SHALL generate fresh 3D meshes for every segmented object without checking the Asset_Warehouse for existing similar assets
2. THE Photo_Pipeline_V14 SHALL NOT implement any similarity matching, hash-based lookup, or semantic deduplication against the Asset_Warehouse before generating meshes
3. WHEN mesh generation completes, THE Photo_Pipeline_V14 SHALL save the new assets to the Asset_Warehouse as additional entries — the warehouse grows monotonically with each session
4. THE pipeline manifest SHALL record a unique session ID and source photo SHA-256 hash to distinguish assets from different submissions, even if the same photo is submitted multiple times

### Requirement 11: GLB Export with Embedded PBR Textures

**User Story:** As a developer, I want all generated GLB files to embed PBR textures compatible with Three.js GLTFLoader, so that meshes render correctly in the V14 web interface without external texture file dependencies.

#### Acceptance Criteria

1. THE Hunyuan3D_Generator and Trellis2_Generator SHALL produce GLB files (binary glTF) with textures embedded as buffer views rather than external file references
2. WHEN Pass 1 photo-projection is applied, THE system SHALL embed the projected texture as a base color map within the GLB's material using the glTF metallic-roughness PBR model
3. WHEN Pass 2 PBR estimation completes, THE system SHALL update the GLB file to include: baseColorTexture, metallicRoughnessTexture, and normalTexture as embedded buffer views following the glTF 2.0 specification
4. THE GLB files SHALL use texture dimensions that are powers of two (256×256, 512×512, or 1024×1024) to ensure WebGL compatibility and efficient GPU memory usage in the V14_Interface
5. FOR ALL GLB files produced by the pipeline, loading the GLB with Three.js GLTFLoader and re-exporting with Three.js GLTFExporter SHALL produce a file whose vertex positions differ by less than 1e-5 from the original (round-trip integrity)

### Requirement 12: Existing Pipeline Preservation and V14 Coexistence

**User Story:** As a developer, I want the V14 pipeline to coexist with all existing interface versions (V3-V13) without breaking any existing functionality.

#### Acceptance Criteria

1. THE Photo_Pipeline_V14 SHALL operate as a new interface version alongside existing versions V3-V13 — all previous versions SHALL remain accessible via their respective `?v=N` query parameters with identical behavior
2. THE WorldContract schema SHALL remain unchanged — the V14 pipeline maps its outputs (real meshes, PBR materials, dynamic physics) into the existing schema fields without requiring schema modifications
3. THE existing UPBGE compilation path, parity gates, smoke validation, and auto-launch chain SHALL remain available for V14 sessions that request UPBGE export, operating on the same WorldContract produced by the V14 pipeline
4. THE V14_Interface SHALL be the default interface version when no `?v=` parameter is supplied, with V13 remaining accessible at `?v=13`
5. THE session management system SHALL distinguish V14 sessions via interface_version=14 in session metadata while using the same FIFO queue, session lifecycle, and TTL cleanup as all other versions

### Requirement 13: Semantic Labeling via Ollama Vision

**User Story:** As a developer, I want each segmented object to receive a semantic label from Ollama vision analysis, so that the Asset Warehouse has meaningful human-readable descriptions and correct category assignments.

#### Acceptance Criteria

1. WHEN an Object_PNG is available, THE system SHALL send the image to Ollama with a structured prompt requesting: semantic_label (e.g., "wooden dining chair"), primary_material (wood/metal/glass/fabric/ceramic/plastic), category (props/architecture/foliage/hard-surface/set-dressing), estimated_era (e.g., "mid-century modern"), condition (new/worn/broken), and is_architectural (boolean)
2. THE Ollama labeling call SHALL use a vision-capable model with flash attention enabled and SHALL complete within 10 seconds per object
3. IF Ollama is unavailable or the response cannot be parsed as valid JSON, THEN THE system SHALL fall back to heuristic labeling based on object dimensions and mask shape: large+rectangular=furniture/props, small+uniform=set-dressing, tall+narrow=architecture
4. THE semantic label SHALL be used to determine: Asset_Warehouse category assignment, material density for physics estimation, and the asset filename in the warehouse
5. THE system SHALL validate that the Ollama response contains all required fields and that category matches one of the five valid taxonomy values before using the response

### Requirement 14: Depth Estimation via Depth Anything 3

**User Story:** As a developer, I want metric depth estimation using Depth Anything 3 (replacing MoGe-2), so that the room shell reconstruction and object placement use the best available depth model.

#### Acceptance Criteria

1. WHEN the Source_Image is provided to the depth estimation stage, THE system SHALL invoke Depth Anything 3 via ComfyUI to produce a metric depth map with values in meters at the same resolution as the Source_Image
2. THE depth estimation stage SHALL load the Depth Anything 3 model only after FLUX has been fully unloaded from VRAM, respecting the VRAM_Manager's sequential loading constraint
3. WHEN the depth map is produced, THE system SHALL validate that at least 50% of pixels have valid depth values (positive, finite, less than 20 meters for indoor scenes)
4. THE depth map SHALL be saved as a float32 NumPy .npy file in the session output directory for use by both room reconstruction and object placement stages
5. IF Depth Anything 3 fails or is unavailable in ComfyUI, THEN THE system SHALL fall back to MoGe-2 if available, then to the flat-floor heuristic (4m depth, width from aspect ratio, 2.7m height) as final fallback

### Requirement 15: Serialization Round-Trip Integrity

**User Story:** As a developer, I want all V14 pipeline artifacts (GLB meshes, asset registry JSON, pipeline manifest) to serialize and deserialize losslessly, so that assets can be reliably loaded by Three.js and pipeline stages can be re-run without data corruption.

#### Acceptance Criteria

1. FOR ALL Asset_Registry JSON files, serializing to JSON then deserializing SHALL produce a structurally equal registry where every field value compares equal (round-trip property)
2. FOR ALL GLB files produced by the Hunyuan3D_Generator or Trellis2_Generator, vertex positions and normals SHALL survive a round-trip through trimesh load/export without exceeding 1e-5 absolute tolerance per component
3. FOR ALL pipeline manifest instances, serializing to JSON (sorted keys, UTF-8) then deserializing SHALL produce a structurally equal manifest
4. FOR ALL depth maps saved as NumPy .npy files, np.save followed by np.load SHALL produce bit-identical float32 arrays
5. THE Asset_Registry JSON format SHALL use sorted keys, 2-space indentation, and UTF-8 encoding for human readability and deterministic diffs across sessions

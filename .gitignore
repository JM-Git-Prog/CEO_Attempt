output/
__pycache__/
*.egg-info/
.pytest_cache/
.hypothesis/
*.pyc
.env

# Vendored MCP dependencies — installed via npm, not committed.
# The Windows patch is preserved in PATCH_NOTES.md + patched-autoloader.js (both tracked).
.kiro/vendor/*/node_modules/

# KiroGraph local index (code graph DB + LanceDB embeddings) — machine-local, rebuildable via `kirograph index`.
.kirograph/

# Large model binaries (tracked outside git)
bench/trained/**/*.safetensors
bench/trained/**/*.gguf
unsloth_compiled_cache/

# E2E test artifacts (per-run outputs, not committed)
tests/e2e/artifacts/

# V14 Asset Warehouse (generated GLB meshes — large binaries, append-only)
assets/

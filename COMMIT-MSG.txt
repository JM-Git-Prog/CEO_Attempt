V17 one box: the model routes every sentence, and the panes finally know each other (2026-09-03/04). New POST /api/v17/say classifies grounds/house/room/check/question/command/gap with the cloud lane and replaces eleven browser regexes plus the second capture-phase submit handler, which were auctioning John's sentences between two brains. minicpm-v now glances at every pasted photo (1.7s) and gemma4:cloud reads a house order out of it; that reading reaches the builder as words instead of being shown and dropped. The room brain is no longer blind - it receives what the photo showed and where John is standing. The world reports its place and room over postMessage; the greeting waits for it and asks rather than assuming. game_message unpinned from llama3.1 to the router. Vision schema widened for column count/colour and roof shape; key-name aliases and essential-only validation so a correct cloud answer is not binned for calling kind "classification" or omitting "reason". Fails closed: unknown is said out loud, never a silent fallback to the room chat.
